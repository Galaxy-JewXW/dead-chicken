# Python环境依赖库
# 电力线提取与三维重建系统所需的Python库
# 
# 安装命令（推荐使用清华源）：
# pip install -i https://pypi.tuna.tsinghua.edu.cn/simple -r requirements.txt

# 核心库
laspy>=2.0.0
numpy>=1.20.0
open3d>=0.15.0
scipy>=1.7.0
scikit-learn>=1.0.0
tqdm>=4.60.0

# 可选库（用于可视化和调试）
matplotlib>=3.5.0

# 其他可能需要的库
pandas>=1.3.0 